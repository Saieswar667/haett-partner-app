services:
  backend:
    build: ./server
    container_name: haett-backend-container
    ports:
      - "5000:5000"

  frontend:
    build: ./client
    container_name: haett-frontend-container
    ports:
      - "5173:5173"
    depends_on:
      - backend